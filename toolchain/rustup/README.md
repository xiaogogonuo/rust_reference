# open《The Rust Programming Language》offline with `rustup`
```shell
rustup docs --book
```

# update to the latest `rust` version
```shell
rustup update
```

# uninstall `rust` and `rustup`
```shell
rustup self uninstall
```